抽象化当前组件 不要有任何具体的内容 任何具体的内容应该尽可能提取出来作为属性(Props)

我指的没有任何具体的内容就是指 不要有任何具体的文字描述 也不要有任何具体的图片描述
例如 不要有任何中文描述 也不要有任何英文描述 也不要有任何具体的图片描述
所有的具体内容都应该作为属性传入组件

然后这些属性都必须有一个最基本最小限度的默认值
比如:
正例:
  如果title字段，是字符串的话，怎给一个空字符串""作为默认值
反例:
  使用"Sample Title" 这种具体的内容

而别的属性也应该有类似的默认值
